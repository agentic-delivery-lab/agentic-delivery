name: agent-observation

# Pull-request lifecycle observations are deliberately separated from the
# agent-invocation workflow. This path validates identity and enrollment only;
# it does not receive App credentials, mutate GitHub state, or start Codex.
on:
  repository_dispatch:
    types: [agent_observation]

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Validate the participant controller pin
        env:
          CONTROLLER_COMMIT: ${{ github.event.client_payload.controller.commit }}
        run: |
          set -euo pipefail
          [[ "$CONTROLLER_COMMIT" =~ ^[0-9a-f]{40}$ ]]

      - name: Check out the pinned observation validator
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
        with:
          ref: ${{ github.event.client_payload.controller.commit }}
          path: controller
          persist-credentials: false

      - name: Set up pnpm and Node.js
        uses: pnpm/setup@84cb39b217b10273981911c288cd62326dc7c2 # v2
        with:
          runtime: node@24
          install: false

      - name: Install observation validator dependencies
        run: pnpm --dir controller install --frozen-lockfile --ignore-scripts

      - name: Validate the signed pull-request observation
        env:
          GITHUB_EVENT_PATH: ${{ github.event_path }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: node controller/scripts/validate-observation-event.mjs controller
