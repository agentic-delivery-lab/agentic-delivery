interface:
  display_name: "Architecture decisions"
  short_description: "Track ADR changes by branch"
  default_prompt: "Use $architecture-decision to manage this ADR decision or removal, link its issue and keep official context limited to main."

policy:
  allow_implicit_invocation: true
