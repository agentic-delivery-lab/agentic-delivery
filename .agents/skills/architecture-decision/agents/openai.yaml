interface:
  display_name: "Architecture Decision"
  short_description: "Draft traceable ADRs from GitHub issues"
  default_prompt: "Use $architecture-decision to evaluate this GitHub issue and draft a proposed ADR."

policy:
  allow_implicit_invocation: true
